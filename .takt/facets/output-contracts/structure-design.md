```markdown
# パッケージ構造設計

## 結果: APPROVE / REJECT

（REJECT の場合は理由を1〜2文で）

## サマリー

{採用した構成の要点を1〜2文}

## 設計目標

| 目標 | 達成方針 |
|------|----------|
| {例: 認知負荷低減} | {1行} |

## 提案ディレクトリ構成（After）

{ツリーまたは箇条書き。`modules/.../src/core/` からの相対が分かること}

## 依存関係（パッケージ／モジュール間）

```text
{例: graph → shape}
{例: stage → graph}
```

- **循環**: なしであること（設計上の根拠を1文）
- **安定度の向き**: {不安定 → 安定 の説明}

## グルーピング根拠

| グループ | 含めるもの | 変更理由（CCP） |
|----------|------------|-----------------|
| {名前} | {サブモジュール列挙} | {1文} |

## 正準パスと廃止対象

| 対象 | 正準パス | 廃止する旧パス | 根拠 |
|------|----------|----------------|------|
| {例: shape ロジック} | {新パス} | {旧パス} | {1文} |

## 命名・公開範囲

| 項目 | 方針 |
|------|------|
| 許容する公開境界 re-export | {場所・対象・理由。旧内部パス互換ではないことを明記} |
| pub の絞り込み | {方針} |
| 既存公開 API の互換 | {破壊的変更の有無 — プロジェクト方針に従う} |

## 禁止する暫定手段

- `#[path = "..."]`
- 旧パス互換の `pub use` ラッパー
- 同一責務の旧パス / 新パスの併存
- import 未更新のまま親 re-export で通すこと

## 型爆発・ボイラープレートの扱い

| 現状 | 提案（マクロ / ジェネリクス / 現状維持） | 理由 |
|------|------------------------------------------|------|
| {例: fan_in_shape2..N} | | |

## 移行に向けた論理ブロック（Phase 候補）

| Phase | 内容 | 完了条件 |
|-------|------|----------|
| A | | |
| B | | |

## リスクと緩和

| リスク | 緩和 |
|--------|------|
| {例: import 大量変更} | {段階移動・コンパイル通過の維持} |

## 分析レポートとの対応

{structure-analysis.md の問題番号ごとに、本設計でどう解消するかを1行ずつ}
```
