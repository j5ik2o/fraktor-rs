# 変更スコープ宣言

## タスク
CircuitBreaker/CircuitBreakerShared にfake clock注入点を追加し、tokio test-util featureを有効化して時間依存テストを決定的にする

## 変更予定
| 種別 | ファイル |
|------|---------|
| 変更 | `modules/actor/src/std/pattern/circuit_breaker.rs` |
| 変更 | `modules/actor/src/std/pattern/circuit_breaker_shared.rs` |
| 変更 | `modules/actor/Cargo.toml` |
| 変更 | `modules/remote/Cargo.toml` |

## 推定規模
Small

## 影響範囲
- circuit_breaker / circuit_breaker_shared のテスト（FakeClock利用のテストが新API `new_with_clock` を呼ぶ）
- scheduler/tick テスト（`start_paused = true` が tokio `test-util` feature を必要とする）
- remote テスト（`start_paused = true` が tokio `test-util` feature を必要とする）
